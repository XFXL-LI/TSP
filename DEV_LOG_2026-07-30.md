# TSP 固件开发日志

## 基本信息

- 日期：2026-07-30
- 固件版本：2.0.2
- 目标硬件：ESP32-S3
- HJ212 DTU：银尔达 M100M-B2
- 开发工程：`TSP`
- 当前阶段：完成修复和编译，等待硬件持续运行测试

## 今日任务

1. 分析实时数据、小时数据偶发缺失的问题。
2. 分析 LCD 操作、历史数据查询、CSQ 查询和 HJ212 上传之间是否存在资源冲突。
3. 修复日志中出现的堆内存持续下降、HJ212 残缺报文和 pending 阻塞问题。
4. 保持现有分钟、小时、天统计和采集计算逻辑不变。
5. 增加诊断日志、固件版本号和变更记录。
6. 使用 Arduino ESP32-S3 工具链完成编译验证。

## 问题现象

测试日志中观察到以下现象：

- 多次操作 LCD 查询数据后，ESP32 可用堆内存持续下降。
- 日志样本中约有 133 次 `get_data` 请求。
- 可用堆内存由约 22 KB 逐渐下降到约 5 KB。
- 内存不足后，HJ212 报文可能退化为约 14 字节的残缺报文。
- 残缺报文进入 pending 后，可能阻塞后续正常数据恢复。
- 低内存状态下可能出现任务创建失败或系统崩溃。
- CSQ 查询与 HJ212 上传共用 `SERIAL_HJ212` 串口及互斥锁，存在调度竞争的可能。

## 根因分析

### 1. 临时响应队列元数据泄漏

LCD/串口命令的 `get_data` 和 `get_records` 请求通过
`PermissionSystem::executeAsyncRoute()` 创建临时响应队列。

固件 2.0.1 中，`EventBus::createReceiverQueue()` 会为每个临时队列向
`queueNames` 写入一条 `"unnamed"` 记录。业务完成后虽然调用了
`vQueueDelete()`，但对应的 `queueNames` 节点没有删除。

因此，每执行一次 LCD 查询都会永久保留一个 `std::map` 节点，最终导致：

```text
LCD 查询次数增加
    → queueNames 持续增长
    → 堆内存持续下降并产生碎片
    → HJ212 String 分配失败
    → 生成残缺报文
    → pending 阻塞及任务创建失败
```

### 2. HJ212 报文缺少完整性保护

原代码在 `String` 内存申请失败时，仍可能继续拼接并返回报文。保存 pending
前也没有统一校验报文长度、帧格式及 CRC，因此残缺报文可能被当作正常报文保存。

### 3. pending 恢复任务压力

原逻辑在每次有效 CSQ 查询后调用 `fileRestore()`。CSQ 周期较短时会频繁扫描
pending 并尝试创建恢复任务，在低内存状态下会进一步增加系统压力。

## 今日代码修改

### 1. 修复 EventBus 临时队列泄漏

涉及文件：

- `src/system/event/eventBus.h`
- `src/system/event/eventBus.cpp`

修改内容：

- 未显式命名的短生命周期队列不再写入 `queueNames`。
- `publish()` 查询队列名称时使用已有映射迭代器，避免每次发布事件都构造临时
  `String`。
- 保留永久任务队列的名称，方便队列满时输出诊断信息。

影响范围：

- LCD `get_data` 请求。
- LCD `get_records` 请求。
- 其他通过异步路由创建的临时响应队列。

### 2. 增加 HJ212 报文构建保护

涉及文件：

- `src/module/pack212/pack212.h`
- `src/module/pack212/pack212.cpp`

修改内容：

- 检查各阶段 `String::reserve()` 的返回结果。
- 减少大段字符串连续拼接产生的临时内存。
- 增加 HJ212 报文完整性校验：
  - `##` 帧头；
  - 4 位数据区长度；
  - 整帧长度；
  - `QN`、`CN` 和 `CP` 关键字段；
  - `&&` 数据区结束标记；
  - CRC；
  - `\r\n` 帧尾。
- 构建失败时返回空报文，不再返回残缺帧。
- 新增 `[DIAG] HJ_BUILD_FAIL` 诊断日志。

说明：

- `pack212.cpp` 原文件使用旧式中文编码，本次统一转换为 UTF-8，业务逻辑之外
  未主动修改原有中文含义。

### 3. pending 保存前校验

涉及文件：

- `src/app/filesysManager/filesysManager.cpp`

修改内容：

- 保存 pending 前调用 HJ212 完整性校验。
- 拒绝保存空报文、14 字节残包或 CRC 错误报文。
- 新增 `[DIAG] PENDING_REJECT` 日志。

### 4. pending 恢复保护

涉及文件：

- `src/system/system/system.cpp`

修改内容：

- 读取到损坏 pending 时输出 `[DIAG] PENDING_INVALID`。
- 优先尝试从 SD 卡原始分钟、小时或天数据重新构建 HJ212 报文。
- 重建成功时输出 `[DIAG] PENDING_REBUILT`。
- 无法重建的损坏文件隔离为 `.invalid`，避免阻塞后续数据。
- 创建恢复任务前检查最大连续可分配内存。
- 内存不足时暂缓恢复并输出 `[DIAG] RECOVERY_DEFER`。
- 恢复任务栈由 8 KB 调整为 5 KB。
- 恢复任务结束日志增加栈剩余量。

### 5. 调整 CSQ 与 pending 扫描频率

涉及文件：

- `src/system/system/system.cpp`

修改内容：

- CSQ 当前设置为每 60 秒查询一次。
- 网络由异常恢复为正常时，立即检查 pending。
- 网络持续正常时，pending 最多每 5 分钟扫描一次。
- CSQ 重试间隔位于串口锁外，避免重试等待期间长期占用串口。

说明：

- 60 秒不是 M100M-B2 的强制要求，而是当前测试阶段的保守配置。
- CSQ 和 HJ212 上传仍共用同一个串口互斥锁。
- “HJ212 上传优先、CSQ 忙时跳过”的统一调度方案暂未加入本版本，待 2.0.2
  日志验证后再决定。

### 6. 更新固件版本

版本由 2.0.1 更新为 2.0.2，涉及：

- `TSP.ino`
- `src/inc/sys_init.h`
- `src/app/configManager/system_json.h`

### 7. 同步发布变更记录

新增：

- `CHANGELOG_2.0.2.md`

## 未修改内容

本次明确未修改：

- 传感器采样计算逻辑。
- 分钟统计逻辑。
- 小时统计逻辑。
- 天统计逻辑。
- SHT30 的 I2C 采集路线。
- LCD 历史记录的请求协议和数据结构。

## LCD 历史查询确认

根据《MCU与屏幕接口文档 V1.0.8》确认：

- 屏幕每次 `get_records` 只查询一个时间点。
- `raw` 每次补取一个缺失分钟。
- `hour` 每次查询一个小时整点。
- `day` 每次查询一个日期零点。
- 同一时刻只允许一个历史事务。
- 每次有效实时轮询最多安排一种历史请求。

因此，当前代码中用于范围查询的循环等待不会被正常屏幕请求触发。屏幕单点查询
仍会创建临时响应队列，所以 EventBus 泄漏修复仍然必要。

## 编译验证

使用 Arduino IDE 自带的 `arduino-cli` 和 ESP32 3.3.7 工具链完成干净编译。

编译结果：

```text
Sketch uses 619172 bytes (19%) of program storage space.
Maximum is 3145728 bytes.

Global variables use 25920 bytes (7%) of dynamic memory,
leaving 301760 bytes for local variables.
Maximum is 327680 bytes.
```

结果：编译通过，无新增编译错误。

## 当前测试任务

### 测试目标

1. 验证连续操作 LCD 后，堆内存不再持续单向下降。
2. 验证 HJ212 实时数据是否连续上传。
3. 验证分钟、小时、天数据是否按原逻辑生成。
4. 验证断网产生 pending 后能够正常恢复。
5. 验证系统不再保存 14 字节残缺报文。
6. 观察 CSQ 查询是否仍会影响实时上传。

### 建议测试操作

1. 烧录 2.0.2 固件并确认启动版本。
2. 正常运行至少 1～2 小时。
3. 多次操作 LCD 实时数据页面。
4. 分别执行若干次 `raw`、`hour`、`day` 单时间点历史查询。
5. 条件允许时短暂断开网络，再恢复网络，观察 pending 补传。
6. 测试结束后导出完整 MCU 日志及 SD 卡目录。

### 日志观察关键字

```text
Firmware version: 2.0.2
[DIAG] TX_ATTEMPT
[DIAG] TX_RESULT
[DIAG] CSQ_LOCK_BUSY
[DIAG] HJ_BUILD_FAIL
[DIAG] PENDING_REJECT
[DIAG] PENDING_INVALID
[DIAG] PENDING_REBUILT
[DIAG] RECOVERY_DEFER
HJ212 serial busy
Queue full
free heap
largest
Guru Meditation
```

## 后续候选任务

以下内容暂未修改，等待本轮日志结果：

### HJ212 串口统一调度

候选目标：

- HJ212 实时上传优先。
- pending 补传次优先。
- CSQ 为低优先级，串口忙时直接跳过。
- 使用发送状态和最近发送结束时间，让 CSQ 与 HJ212 数据帧保持保护间隔。
- 所有 HJ212 数据和 DTU 控制命令由同一个调度入口处理。

采用该方案前，需要先确认当前日志中是否确实存在：

- CSQ 查询附近出现 `HJ212 serial busy`；
- 实时数据因获取串口锁失败转入 pending；
- CSQ 和 HJ212 上传周期性同步碰撞。

## 今日结论

本次首先解决了已经由日志证明的内存泄漏和残缺报文问题，并增加了 pending
自恢复保护。当前代码已通过编译，进入硬件运行验证阶段。

在取得 2.0.2 的连续运行日志前，不叠加 HJ212 串口统一调度改动，以便准确判断
本轮修复效果。
