# Firmware 2.0.4：2026-08-10实机验证包

这是2026-08-10 16:33完成构建、随后烧录到MAC `e0:72:a1:d2:55:c4` 并通过首轮
实机验证的四段固件。

验证已覆盖：写入Hash、正常启动、RTC保留秒、连续8次严格60秒采集、SD raw唯一保存、
上传优先级避让，以及一次无ACK pending恢复。尚未覆盖完整24小时周期校时和长期小时20点
输入验证。

常规烧录必须使用四段地址，不使用merged.bin，不执行全片擦除，以保留从 `0x00610000`
开始的FFat配置区。建议通过 `scripts/Flash-Firmware.ps1` 执行，其会先校验全部SHA-256并要求
显式确认词。
