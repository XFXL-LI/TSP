# 状态转换
1. 能够将监测数据在 “标准状态 - 参比状态”  默认配置（标准气压）自设配置
2. 质量浓度（?g/m?、mg/m?）与体积浓度（ppm、ppb）转换

# 断点续传
3. 数据报警后进行处理，主要为网络，无法传输后进行补传
4. 仪器掉电后，能自动保存当前数据和运行状态；恢复供电后，系统应能自动启动，并从中断点恢复运行与数据记录
5. 在网络中断期间，设备应能继续在本地存储数据；待网络恢复后，自动将缓存的数据补传至服务器，确保数据的连续性。

# 温度控制 / 报警监测 / 系统监测 / 自检功能
6. 温度湿度低于指定范围后进行加热
7. 监控采样泵状态、传感器工作电压等。
8. 长时间没有数据采集报警
9. 长时间无网络报警
10. 电池电压采集报警
11. 开机或定期进行系统自检，报告各单元健康状况

# 操作日志
12. 配置文件修改日志储存
13. 系统文件修改储存
14. 标定数据存储
15. 记录仪器的零点漂移、量程漂移校准记录，以及设备报警、停电等异常状态 。
16. 提供界面或接口，供用户查询历史时间段内的监测数据、校准记录、事件日志等
17. 校准证书所需信息如客户信息、校准依据、校准结果、测量不确定度等，需要软件能够生成或支持生成包含这些信息的格式化报告。
18. 能以报表或报告形式输出数据
19. 记录所有参数修改（尤其是 K 值、校准值）的操作人及时间。
20. 支持网络校时或手动校时，确保所有数据的时间标签（YYYYMMDDHHMM）精确统一。
21. 导出 Excel/PDF 格式的小时报表、日报表、月报表。
22. 每5分钟记录一次系统的工作状态信息，如采样流量、温度、压力、电源状态等，能够检测传感器故障、通信中断、电源异常、校准失败等情况，并产生相应的报警信息或事件记录
23. 能够对数据进行标记，例如标记维护、校准、故障、电源中断、超限等状态
24. 软件应对所有人员的关键控制操作（如参数修改、校准执行、关机重启等）进行自动记录、保存，形成不可修改的系统操作记录日志，并可查询
25. 参数修改留痕：任何对关键运行参数、校准系数的修改，都必须被记录。记录信f息应包括：被修改的参数名称、修改者、修改时间、修改前的值、修改后的值
26. 记录浓度超标、采样流量异常、通讯中断、断电等事件。

# 系统设置：
27. 能够显示和设置系统时间、采样参数（如采样周期）、报警阈值等。
28. 能够显示仪器内部工作参数
29. 具备内部实时时钟，用于为所有数据记录、事件日志打上时间标签。
30. 支持通过网络或其他方式对系统时钟进行同步，确保时间准确性。
31. 设置不同级别的用户账号（如操作员、管理员、校准员），并分配不同的操作权限（如查看数据、修改参数、执行校准、用户管理等），防止未授权操作。
32. 显示产品名称、型号、制造日期及制造厂等铭牌信息
33. 零点校准、量程校准，自动定时校准
