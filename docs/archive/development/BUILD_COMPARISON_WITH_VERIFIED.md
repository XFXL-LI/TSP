# 与2026-08-10实机版本的比较

统一脚本重新编译成功：Sketch 623296字节、动态内存25936字节、应用BIN 623440字节，
三项均与当日实机版本一致。

新BIN SHA-256为 `3CCDDEC7B809C2DE7007861C56B16B481F7E8DC10F2BDC7E4A9B76BC37D7E309`，
实机版本为 `6CE8B44EA4BF509815A7CE57470B1CC79CA84F85355BF6E4F2D1CB6EA13A4773`。

逐字节比较共69字节不同：

- 5字节是ESP32 Core写入固件的编译时间，从 `16:33:20` 变为 `17:58:12`；
- 其余64字节分别是随内容变化的两个32字节摘要区域；
- 长度、程序/内存占用和源文件指纹一致，没有发现业务源码差异。
- 当前 `src` 下75个 `.cpp/.c/.h/.hpp` 文件与16:33验证构建目录保存的编译副本逐一
  比较；去除Arduino构建器自动加入的首行 `#line` 后，75个文件内容全部一致。

因此最新构建与实机版本的功能源码一致，但仍按规范标记为“未实机验证”；默认烧录入口继续
指向 `releases/2.0.4/2026-08-10_hw-verified`。
