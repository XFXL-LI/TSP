# Firmware 2.0.4

发布日期：2026-07-31

## 修改依据

- Firmware 2.0.3 硬件日志显示，逐分钟采集和 SD 保存完整，但 CSQ 命令返回
  `99` 后会覆盖上一次有效值，导致数分钟实时数据没有立即尝试上传。
- LED 接收队列曾达到 `waiting=5 spaces=0`，并持续保留5个处理包，与可用堆
  阶梯式下降的现象一致。
- pending 文件结构完整，没有发现前部零字节。
- 小时文件和 HJ212 小时包均能正常生成。

## 修改内容

- 无效 CSQ 不再覆盖上一次有效 CSQ，并新增 `CSQ_INVALID_KEEP` 诊断标记。
- CSQ 瞬时失败后，实时 HJ212 仍允许尝试上传；实际报文 ACK 决定是否写入
  pending。
- RS485 LED 只处理实时包。分钟、小时和日包立即释放并记录
  `LED_SKIP_NONREAL`，避免统计包占用完整显示周期并填满 LED 队列。
- HJ212 逐包测试间隔保持3000毫秒。

## 2.0.4 硬件复测后的补充修正

- 16:28–17:21 的复测中，CSQ 无效值被正确保留，实时采集和 SD 文件连续，
  LED 非实时包能够释放。
- 同时发现 CSQ 查询与实时上传均为60秒周期。一次碰撞后，CSQ 每次延后仍等待
  完整60秒，可能长期与上传保持同相位，导致五分钟 pending 维护扫描停止。
- `CSQ_DEFERRED` 或无效 CSQ 现在改为5秒后重试；健康状态下的正常 CSQ
  查询仍保持60秒周期。
- pending 五分钟维护在取得过一次有效 CSQ 后独立计时，不再依赖本次 CSQ
  命令成功；实际补传仍由 `DTUManager` 统一仲裁串口。

## 兼容性说明

- LCD `get_data` 行为和查询间隔保持不变。
- LED 显示内容和485报文格式保持不变；总显示预算由完整采集周期调整为
  预留5秒处理余量。
- 分钟、小时和日统计的采集、计算、边界结算、存储、报文内容和分发规则
  均未修改。
- 完整 pending 报文格式和 SD 写后校验保持不变；新增仅用于构造失败恢复的
  `REBUILD_FROM_SD` 标记。

## 2026-08-03 长时间测试后的修复

- 修复采集无效时 `SystemRuntimeStatus` 报警对象未释放造成的持续堆内存泄漏。
- PM1、PM2.5 采集无效时新增 `PM_COLLECT_INVALID` 诊断，区分 Modbus
  读取失败、成功读到0以及异常寄存器值；本次不改变“0值无效”的原有规则。
- LED 使用55秒总显示预算，并在开始新一轮显示前合并积压快照，只保留最新
  实时数据，防止显示队列长期占用多份数据包内存。
- HJ212 报文因临时低内存无法构造时写入重建标记，维护任务随后从对应 SD
  数据文件重新构造并补传。
- CSQ、HJ212 3000毫秒逐包间隔以及分钟、小时、日统计逻辑均未修改。

## 2026-08-04 FFat 配置写入修正

- 修正配置保存调用方对 `writeFFAT()` 成功返回值的判断：成功值统一按 `0`
  处理，不再错误判断为 `3`。
- FFat 写入后核对实际写入字节数与预期字节数；不一致时返回失败并记录
  `FFAT write incomplete`。
- 空配置内容在删除旧文件前直接拒绝，避免空内容覆盖已有配置。
- 写入成功时记录配置路径和实际字节数，便于通过串口日志确认。
- 新板默认配置中，风速、风向和气压的 `alarmLimit` 设为 `999`；气压单位
  修正为 `kPa`。已有 FFat 配置仍优先于这些源码默认值。
- 设备信息默认版本号由 `2.0.3` 同步为 `2.0.4`，与固件版本宏保持一致；该项
  只修正查询显示，不改变采集、统计、保存或上传逻辑。
- 2026-08-04 14:49 Arduino IDE 编译成功：Sketch 622584 字节，动态内存
  25936 字节；随后通过 COM9 烧录成功，全部写入段 Hash 校验通过。
- 已核对 Arduino IDE 本次构建副本和固件二进制，确认上述 FFat 返回值及
  字节数检查已进入烧录固件。
- 本次普通程序烧录最高擦写到 `0x000A8FFF`，FFat 分区从 `0x00610000`
  开始，因此未擦除板内已有 FFat 配置。
- 2026-08-04 15:00 已通过大彩 HMI 屏保存传感器配置，串口记录
  `FFAT write success: path=/model.json bytes=577`；随后执行软件重启，固件成功
  解析并加载 `/model.json`，10 个传感器配置全部恢复，写入、字节数判断和
  重启回读验证通过。
- 本次修正未修改小时统计、HJ212 3000毫秒逐包间隔或 SHT30 代码。

## 2026-08-04 正式运行日志调整

- 关闭源码中的 `DEBUG` 详细日志开关，正式运行使用 `INFO` 日志级别。
- 不再输出传感器原始采样、完整大彩 HMI 屏 JSON、对象释放、队列计数、
  LED 灯珠灯箱轮显周期等高频调试信息。
- 保留固件版本、启动状态、采集/处理/存储/HJ212 诊断、FFat 保存结果以及
  `WARNING`、`ERROR`、`CRITICAL` 日志，便于现场判断关键故障。
- 该调整仅改变日志过滤级别，不改变采集、统计、报警、屏幕通信、SD 保存、
  pending 恢复或 HJ212 发送逻辑。
- 用户随后因实时数据表现疑问，于 18:07 在实际源码中重新启用 `DEBUG` 并
  烧录；当前正式运行固件为 DEBUG 开启状态。新会话不要按“INFO 已关闭
  DEBUG”的旧状态继续处理。

## 2026-08-04～08-05 正式运行结论

- 18:09～次日 09:28 当前启动周期 raw 连续 920 分钟；日志中
  `trace=2～920` 无缺号，实时、十分钟、小时和日共 1027 个处理包全部完成
  SD 保存并取得最终发送结果。
- SD 共 1034 个数据文件，逐文件检查均为 400 字节、10 个完整有效因子，
  未发现损坏、零填充或本地漏存。
- 84 个最终无 ACK 的包全部写入 pending；其中 83 个随后恢复，当前只遗留
  `pending/0/20260805092020.pkt`。该文件长度、HJ212内容、CRC和对应 raw
  数据均已验证正确，不得删除。
- 网络 ACK 存在持续波动，但未导致采集、处理或 SD 数据缺失；平台最终入库
  完整性仍需平台导出数据确认。
- 堆内存能够恢复，最大连续块约 7668 字节；无崩溃、重启、队列压力、事件
  丢弃、存储失败、pending 校验失败或 HJ212 构包失败。
- SHT30 在本段连续出现 1779 次 `-101`，继续独立处理，不并入数据稳定性
  代码修改。

## 2026-08-05 开发板硬件与 DEBUG 核查

- 确认实际开发板购买型号为 `ESP32-S3-N16R8（带天线座）_焊接`，实物属于
  44 针、双 Type-C、外置天线座的 ESP32-S3-DevKitC/YD 系兼容板。
- 实测确认板载四脚可寻址 RGB 状态灯的数据输入经零欧姆电阻连接 GPIO48；
  RGB 灯右下角接 GND，电源实测约为 4.2 V。
- 当前固件使用 `GPIO48` 作为 1 位 SDMMC 的 CMD，`GPIO47` 为 CLK，
  `GPIO21` 为 DAT0，因此板载 RGB 数据输入与 SD CMD 共线。
- 该 RGB 输入支路可能形成额外负载，但正式测试中的 SD 连续保存已经通过，
  目前没有证据证明共线造成 SD 初始化或数据保存异常。当前不拆零欧姆电阻、
  不改 SD 引脚、不控制板载 RGB，仅保留为后续硬件对照项。
- 再次核对 `DEBUG` 宏只切换 `LogManager` 的 DEBUG/INFO 过滤级别，没有包住
  HJ212 上传或其他业务代码；关键链路诊断仍为 INFO 以上。今日实际源码仅新增
  DEBUG 说明注释和空白差异，`#define DEBUG` 仍然生效，没有业务逻辑变更。
- 注释 `DEBUG` 不会直接关闭数据上传，但会减少串口输出并改变部分运行时序；
  当前正式测试继续保持 DEBUG 开启，不在测试中途切换固件基线。

## 2026-08-10 分钟时间轴缺口修复

- 串口日志 `20260805-18.03.log` 覆盖 2026-08-06 10:58～2026-08-10 13:36。
  日志中的 5911 次实时采集 trace 连续，6606 个实时/十分钟/小时/日处理包全部
  对应 `STORE ok=1`；不存在任务周期丢失或 SD 写入失败证据。
- `D:\TSP-SD-TestData` 共核验 7332 个数据文件、73370 条二进制记录，文件结构、
  时间戳、10 因子集合及浮点数据均完整；日志中的 6606 次成功保存全部能在 SD
  副本中找到。
- 完整 SD 时段内发现 13 个 raw 分钟路径缺失和 5 个重复分钟文件。日志覆盖范围
  内为 10 个缺失分钟和 3 个重复分钟；重复文件均为两批完整记录追加形成的
  800 字节文件，不是介质损坏。
- 根因一：`CollectTask` 在 `vTaskDelayUntil()` 已推进 `xLastWakeTime` 后，又把它
  重置为实际唤醒 tick，使调度迟延逐周期累计。实测采集周期平均约 60.08 秒，
  当相位从上一分钟的 59 秒跨越时会跳过一个分钟路径。
- 根因二：RTC/DTU 原本解析到秒，却只生成 12 位 `YYYYMMDDhhmm` 时间；周期校时
  随后固定设置 `tm_sec=0`。日志已证明该归零校时直接造成 2 个缺失分钟和
  3 个重复分钟。
- 修复 `CollectTask`：删除 `vTaskDelayUntil()` 后对 `xLastWakeTime` 的二次重置，
  让 FreeRTOS 使用固定周期基准，避免调度迟延累计。
- 修复时间链路：DTU 时间保留秒并生成 14 位 `YYYYMMDDhhmmss`；RTC 读取保留
  `DateTime.second`；`timeInit()`、`updateMillisTime()` 和 `isValidClockTime()`
  同时兼容旧12位时间和新14位时间。
- 小时文件本身没有缺失，109 个已闭合小时文件均存在且与现有算法逐字段一致；
  其中 3 个小时因上游缺失的 `11:27`、`00:18`、`18:24` 仅使用19个样本。
  本次未修改小时统计的“三分钟取样”规则，修复 raw 分钟时间轴后由上游消除影响。
- `system.cpp`、`dtuManager.cpp` 已使用原项目 ESP32 3.3.7 编译参数分别编译通过。
  本次没有烧录；仍需新固件上机后连续运行，验证 raw 每分钟唯一、无缺失且
  小时输入恢复为正常20个采样点。
- HJ212 逐包间隔继续保持 3000ms，DEBUG 继续开启；SHT30 和大彩HMI屏逻辑均未修改。
- 当前 SD 卡及副本均没有 pending 文件。旧的 `20260805092020.pkt` 在现有日志中
  没有恢复/删除记录，因此只记录为“当前不存在”，不能补记为已证明成功恢复。

### DTU 网络校时时序优化

- 启动阶段不再阻塞等待 DTU 时间命令及重试；DS1302 RTC 立即初始化系统时间，
  保证采集任务优先启动。取得第一次有效CSQ后约5秒进入首次网络时间校准尝试，
  首次尝试完成后恢复正常60秒CSQ周期。
- 运行期网络校准改用 `millis()` 计算真实间隔，每24小时一次，不再使用受
  CSQ成功、5秒延迟和60秒延迟影响的循环计数器。
- 网络取时失败或因HJ212上传繁忙而延后时，5分钟后重试；到期不会阻塞实时或
  pending HJ212报文。
- `hjSystemTime()` 现在与CSQ命令采用相同的上传优先级检查：存在上传等待、
  上传进行中、上传结束不足1500ms或串口锁繁忙时，记录 `TIME_SYNC_DEFER`
  并立即返回。串口锁等待由3000ms缩短为100ms。
- 校时前等待系统秒进入10～40秒安全区间，避开分钟边界。网络时间与系统时间
  偏差不超过3秒时只记录 `TIME_SYNC_VERIFIED`，不重设系统和RTC；偏差超过3秒
  才更新两者并记录 `TIME_SYNC_APPLIED`。
- 优化后的 `system.cpp`、`dtuManager.cpp` 已再次按ESP32 3.3.7原项目参数分别
  编译通过；未烧录MCU。

## 后续硬件验证

- 确认 `CSQ_DEFER` 后约5秒能够重新查询，而不是每分钟重复碰撞。
- 平台侧核对 `20260805092020` 是否已经入库；当前文件已经不存在，但现有日志
  不能证明其具体消失原因。
- 继续观察可用堆和最大连续块在多小时运行中保持稳定。
- 确认小时文件和 `type=2` 上传规则保持不变。
